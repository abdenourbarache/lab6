apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab7.fullname" . }}
  labels:
    {{- include "lab7.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab7.selectorLabels" . | nindent 4 }}