apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab6.fullname" . }}
  labels:
    {{- include "lab6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab6.selectorLabels" . | nindent 4 }}